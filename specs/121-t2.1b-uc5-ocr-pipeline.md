# T2.1b — Faza 2: UC5 OCR Pipeline — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Phase:** 2 · **Track ID:** T2.1b · **Procijenjeno:** 1 nedjelja
**UC pokriveni:** UC5 alt 5a/5b, UC6
**Spec referenca:** sekcije 5.3, 7.1, 7.4.2, 9.4, 10.3, 11.5
**Blokira:** T2.1c · **Blokiran-od:** T1.3 (FakeOcrAdapter, PrivateFileStorage)

**Goal:** Profesor uploaduje ljekarsku potvrdu za člana ekipe → background job pokreće `OcrAdapter` → status `pending → valid/expired/invalid/manual_review`. UI prikazuje stanje.

**Architecture:** `MedicalCertificateController::store` → `PrivateFileStorage::storeFor` → `MedicalCertificate::create(status=pending)` → dispatch `ValidateMedicalCertificateJob($certId)` na `ocr` queue. Job poziva `OcrAdapter::extract`, transition-uje status. Inertia partial reload (preferiraj nad polling-om) na promjenu statusa. Scheduled `ExpireMedicalCertificatesJob` daily 02:00.

---

## Pre-flight

- [ ] T1.3 (FakeOcrAdapter, PrivateFileStorage) mergovan.
- [ ] Worktree: `git worktree add ../sportski-savez-app-t2.1b -b feature/t2.1b-uc5-ocr`.

---

## Task 1: `MedicalCertificatePolicy`

**Files:**
- Create: `app/Policies/MedicalCertificatePolicy.php`
- Test: `tests/Feature/Policies/MedicalCertificatePolicyTest.php`

- [ ] **Step 1: Failing test**

```php
<?php // tests/Feature/Policies/MedicalCertificatePolicyTest.php

use App\Models\MedicalCertificate;
use App\Models\Professor;
use App\Models\Student;
use App\Models\Team;
use App\Models\TeamMember;
use App\Models\User;

it('professor can upload cert for own team member', function () {
    $prof = Professor::factory()->create();
    $team = Team::factory()->create(['professor_id' => $prof->id]);
    $member = TeamMember::factory()->create(['team_id' => $team->id]);

    expect($prof->can('upload', $member))->toBeTrue();
});

it('professor cannot upload cert for other team member', function () {
    $other = Professor::factory()->create();
    $team = Team::factory()->create(['professor_id' => $other->id]);
    $member = TeamMember::factory()->create(['team_id' => $team->id]);

    $mine = Professor::factory()->create();
    expect($mine->can('upload', $member))->toBeFalse();
});

it('admin can manual approve manual_review certs', function () {
    $admin = User::factory()->admin()->create();
    $cert = MedicalCertificate::factory()->create(['status' => 'manual_review']);

    expect($admin->can('manualApprove', $cert))->toBeTrue();
});

it('non-admin cannot manual approve', function () {
    $prof = Professor::factory()->create();
    $cert = MedicalCertificate::factory()->create(['status' => 'manual_review']);
    expect($prof->can('manualApprove', $cert))->toBeFalse();
});

it('student can view own cert', function () {
    $student = Student::factory()->create();
    $member = TeamMember::factory()->create(['student_id' => $student->id]);
    $cert = MedicalCertificate::factory()->create(['team_member_id' => $member->id]);

    expect($student->can('view', $cert))->toBeTrue();
});
```

- [ ] **Step 2: Policy**

```php
<?php // app/Policies/MedicalCertificatePolicy.php

namespace App\Policies;

use App\Models\MedicalCertificate;
use App\Models\TeamMember;
use App\Models\User;

class MedicalCertificatePolicy
{
    public function view(User $user, MedicalCertificate $cert): bool
    {
        if ($user->isAdmin()) {
            return true;
        }

        $member = $cert->teamMember;

        if ($user->isStudent()) {
            return $member->student_id === $user->id;
        }

        if ($user->isProfessor()) {
            return $member->team->professor_id === $user->id;
        }

        return false;
    }

    public function upload(User $user, TeamMember $member): bool
    {
        if ($user->isAdmin()) {
            return true;
        }

        return $user->isProfessor() && $member->team->professor_id === $user->id;
    }

    public function delete(User $user, MedicalCertificate $cert): bool
    {
        return $this->view($user, $cert);
    }

    public function manualApprove(User $user, MedicalCertificate $cert): bool
    {
        return $user->isAdmin() && $cert->status->value === 'manual_review';
    }
}
```

- [ ] **Step 3: Run, PASS, commit**

```powershell
php artisan test --compact --filter=MedicalCertificatePolicyTest
vendor/bin/pint --dirty --format agent
git add app/Policies/MedicalCertificatePolicy.php tests/Feature/Policies/MedicalCertificatePolicyTest.php
git commit -m "T2.1b: MedicalCertificatePolicy per spec 13.4"
```

---

## Task 2: State machine helperi na `MedicalCertificate` model

**Files:**
- Modify: `app/Models/MedicalCertificate.php`
- Test: `tests/Unit/MedicalCertificateStateTest.php`

- [ ] **Step 1: Failing test**

```php
<?php // tests/Unit/MedicalCertificateStateTest.php

use App\Enums\MedicalCertificateStatus;
use App\Models\MedicalCertificate;

it('pending → valid allowed', function () {
    $cert = MedicalCertificate::factory()->pending()->create();
    $cert->markValid(now()->addYear(), 'Petar Petrović', 0.95);

    expect($cert->fresh()->status)->toBe(MedicalCertificateStatus::Valid);
});

it('valid → pending throws', function () {
    $cert = MedicalCertificate::factory()->create();
    expect(fn () => $cert->transitionTo(MedicalCertificateStatus::Pending))
        ->toThrow(\DomainException::class);
});

it('any → superseded when replaced', function () {
    $cert = MedicalCertificate::factory()->create();
    $cert->markSuperseded();
    expect($cert->fresh()->status)->toBe(MedicalCertificateStatus::Superseded);
});
```

- [ ] **Step 2: Dodaj helpere u model**

```php
// app/Models/MedicalCertificate.php — append metode

use App\Enums\MedicalCertificateStatus;
use Carbon\Carbon;

public function transitionTo(MedicalCertificateStatus $next): void
{
    if (! in_array($next, $this->status->nextStates(), true)) {
        throw new \DomainException("Invalid transition from {$this->status->value} to {$next->value}.");
    }
    $this->update(['status' => $next]);
}

public function markValid(Carbon $expiresAt, string $extractedName, float $confidence): void
{
    $this->update([
        'status' => MedicalCertificateStatus::Valid,
        'expires_at' => $expiresAt,
        'extracted_name' => $extractedName,
        'ocr_confidence' => $confidence,
    ]);
}

public function markExpired(): void
{
    $this->update(['status' => MedicalCertificateStatus::Expired]);
}

public function markInvalid(string $reason = ''): void
{
    $this->update([
        'status' => MedicalCertificateStatus::Invalid,
        'ocr_raw_response' => $reason,
    ]);
}

public function markManualReview(string $reason = ''): void
{
    $this->update([
        'status' => MedicalCertificateStatus::ManualReview,
        'ocr_raw_response' => $reason,
    ]);
}

public function markSuperseded(): void
{
    $this->update(['status' => MedicalCertificateStatus::Superseded]);
}
```

- [ ] **Step 3: Run, PASS, commit**

```powershell
php artisan test --compact --filter=MedicalCertificateStateTest
vendor/bin/pint --dirty --format agent
git add app/Models/MedicalCertificate.php tests/Unit/MedicalCertificateStateTest.php
git commit -m "T2.1b: MedicalCertificate state machine helpers"
```

---

## Task 3: `ValidateMedicalCertificateJob`

**Files:**
- Create: `app/Jobs/ValidateMedicalCertificateJob.php`
- Test: `tests/Feature/Jobs/ValidateMedicalCertificateJobTest.php`

- [ ] **Step 1: Failing test**

```php
<?php // tests/Feature/Jobs/ValidateMedicalCertificateJobTest.php

use App\Enums\MedicalCertificateStatus;
use App\Jobs\ValidateMedicalCertificateJob;
use App\Models\MedicalCertificate;

it('marks certificate valid for proper filename and future date', function () {
    $cert = MedicalCertificate::factory()->pending()->create([
        'original_filename' => 'petar_petrovic_2028-12-31.pdf',
        'path' => 'fake/path.pdf',
    ]);

    ValidateMedicalCertificateJob::dispatchSync($cert->id);

    expect($cert->fresh()->status)->toBe(MedicalCertificateStatus::Valid);
});

it('marks expired for past filename date', function () {
    $cert = MedicalCertificate::factory()->pending()->create([
        'original_filename' => 'ana_anic_2020-01-01.pdf',
        'path' => 'fake/path.pdf',
    ]);

    ValidateMedicalCertificateJob::dispatchSync($cert->id);

    expect($cert->fresh()->status)->toBe(MedicalCertificateStatus::Expired);
});

it('marks manual_review when OCR confidence is low', function () {
    $cert = MedicalCertificate::factory()->pending()->create([
        'original_filename' => 'random_garbage.pdf',
        'path' => 'fake/path.pdf',
    ]);

    ValidateMedicalCertificateJob::dispatchSync($cert->id);

    expect($cert->fresh()->status)->toBe(MedicalCertificateStatus::ManualReview);
});
```

- [ ] **Step 2: Generiši i implementiraj**

```powershell
php artisan make:job ValidateMedicalCertificateJob
```

```php
<?php // app/Jobs/ValidateMedicalCertificateJob.php

namespace App\Jobs;

use App\Contracts\OcrAdapter;
use App\Models\MedicalCertificate;
use App\Services\AuditLogger;
use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Foundation\Bus\Dispatchable;
use Illuminate\Queue\InteractsWithQueue;
use Illuminate\Queue\SerializesModels;

class ValidateMedicalCertificateJob implements ShouldQueue
{
    use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;

    public int $tries = 3;
    public int $backoff = 30; // sekundi exp backoff faktor

    public function __construct(public int $certificateId)
    {
        $this->onQueue('ocr');
    }

    public function handle(OcrAdapter $ocr, AuditLogger $audit): void
    {
        $cert = MedicalCertificate::find($this->certificateId);
        if (! $cert || $cert->status->value !== 'pending') {
            return; // možda već procesiran (race)
        }

        $result = $ocr->extract($cert->path, $cert->original_filename);

        if ($result->needsManualReview()) {
            $cert->markManualReview('OCR confidence too low: '.$result->confidence);
            $audit->log('certificate.manual_review', $cert, $result->toArray());

            return;
        }

        if ($result->expiresAt === null || $result->extractedName === null) {
            $cert->markInvalid('OCR returned incomplete data');
            $audit->log('certificate.invalidated', $cert, $result->toArray());

            return;
        }

        if ($result->isExpired()) {
            $cert->update([
                'expires_at' => $result->expiresAt,
                'issued_at' => $result->issuedAt,
                'extracted_name' => $result->extractedName,
                'ocr_confidence' => $result->confidence,
                'status' => \App\Enums\MedicalCertificateStatus::Expired,
            ]);
            $audit->log('certificate.expired', $cert, $result->toArray());

            return;
        }

        $cert->markValid($result->expiresAt, $result->extractedName, $result->confidence);
        $cert->update(['issued_at' => $result->issuedAt]);
        $audit->log('certificate.validated', $cert, $result->toArray());
    }
}
```

- [ ] **Step 3: Run, PASS, commit**

```powershell
php artisan test --compact --filter=ValidateMedicalCertificateJobTest
vendor/bin/pint --dirty --format agent
git add app/Jobs/ValidateMedicalCertificateJob.php tests/Feature/Jobs/
git commit -m "T2.1b: ValidateMedicalCertificateJob with OCR adapter dispatch"
```

---

## Task 4: `MedicalCertificateController` (upload, preview, replace)

**Files:**
- Create: `app/Http/Controllers/MedicalCertificateController.php`
- Create: `app/Http/Requests/StoreMedicalCertificateRequest.php`
- Modify: `routes/teams.php`
- Test: `tests/Feature/MedicalCertificate/UploadTest.php`

- [ ] **Step 1: Failing test**

```php
<?php // tests/Feature/MedicalCertificate/UploadTest.php

use App\Models\MedicalCertificate;
use App\Models\Professor;
use App\Models\Team;
use App\Models\TeamMember;
use Illuminate\Http\UploadedFile;
use Illuminate\Support\Facades\Bus;
use Illuminate\Support\Facades\Storage;

beforeEach(function () {
    Storage::fake('private');
    Bus::fake();
    $this->prof = Professor::factory()->create();
    $this->team = Team::factory()->create(['professor_id' => $this->prof->id]);
    $this->member = TeamMember::factory()->create(['team_id' => $this->team->id]);
});

it('uploads valid PDF and dispatches OCR job', function () {
    $file = UploadedFile::fake()->create('petar_petrovic_2028-12-31.pdf', 500, 'application/pdf');

    $this->actingAs($this->prof)
        ->post("/teams/{$this->team->id}/members/{$this->member->id}/certificate", [
            'file' => $file,
        ])
        ->assertRedirect();

    $cert = MedicalCertificate::where('team_member_id', $this->member->id)->first();
    expect($cert)->not->toBeNull();
    expect($cert->status->value)->toBe('pending');
    expect($cert->original_filename)->toBe('petar_petrovic_2028-12-31.pdf');

    Bus::assertDispatched(\App\Jobs\ValidateMedicalCertificateJob::class);
});

it('rejects file over 10 MB', function () {
    $file = UploadedFile::fake()->create('x.pdf', 11000, 'application/pdf');

    $this->actingAs($this->prof)
        ->post("/teams/{$this->team->id}/members/{$this->member->id}/certificate", [
            'file' => $file,
        ])
        ->assertSessionHasErrors('file');
});

it('rejects non-allowed MIME', function () {
    $file = UploadedFile::fake()->create('x.exe', 100, 'application/octet-stream');

    $this->actingAs($this->prof)
        ->post("/teams/{$this->team->id}/members/{$this->member->id}/certificate", [
            'file' => $file,
        ])
        ->assertSessionHasErrors('file');
});

it('replacing certificate supersedes old', function () {
    $oldCert = MedicalCertificate::factory()->create(['team_member_id' => $this->member->id]);

    $file = UploadedFile::fake()->create('marko_markovic_2028-01-01.pdf', 500, 'application/pdf');
    $this->actingAs($this->prof)
        ->post("/teams/{$this->team->id}/members/{$this->member->id}/certificate", [
            'file' => $file,
        ])
        ->assertRedirect();

    expect($oldCert->fresh()->status->value)->toBe('superseded');
});

it('other professor cannot upload', function () {
    $other = Professor::factory()->create();
    $file = UploadedFile::fake()->create('x.pdf', 100, 'application/pdf');

    $this->actingAs($other)
        ->post("/teams/{$this->team->id}/members/{$this->member->id}/certificate", [
            'file' => $file,
        ])
        ->assertForbidden();
});
```

- [ ] **Step 2: Form Request**

```php
<?php // app/Http/Requests/StoreMedicalCertificateRequest.php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class StoreMedicalCertificateRequest extends FormRequest
{
    public function authorize(): bool
    {
        return $this->user()->can('upload', $this->route('member'));
    }

    /** @return array<string, mixed> */
    public function rules(): array
    {
        return [
            'file' => [
                'required',
                'file',
                'max:10240',
                'mimes:pdf,jpg,jpeg,png',
            ],
        ];
    }
}
```

- [ ] **Step 3: Controller**

```php
<?php // app/Http/Controllers/MedicalCertificateController.php

namespace App\Http\Controllers;

use App\Enums\MedicalCertificateStatus;
use App\Http\Requests\StoreMedicalCertificateRequest;
use App\Jobs\ValidateMedicalCertificateJob;
use App\Models\MedicalCertificate;
use App\Models\Team;
use App\Models\TeamMember;
use App\Services\AuditLogger;
use App\Services\PrivateFileStorage;
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Response;
use Inertia\Inertia;

class MedicalCertificateController extends Controller
{
    public function __construct(
        private AuditLogger $audit,
        private PrivateFileStorage $storage,
    ) {}

    public function store(StoreMedicalCertificateRequest $request, Team $team, TeamMember $member): RedirectResponse
    {
        if ($member->team_id !== $team->id) {
            abort(404);
        }

        // supersede stari ako postoji
        $existing = $member->medicalCertificate;
        if ($existing && $existing->status !== MedicalCertificateStatus::Superseded) {
            $existing->markSuperseded();
            $this->audit->log('certificate.superseded', $existing);
        }

        $path = $this->storage->storeFor($member, $request->file('file'), '');

        $cert = MedicalCertificate::create([
            'team_member_id' => $member->id,
            'original_filename' => $request->file('file')->getClientOriginalName(),
            'path' => $path,
            'status' => MedicalCertificateStatus::Pending,
        ]);

        $this->audit->log('certificate.uploaded', $cert);
        ValidateMedicalCertificateJob::dispatch($cert->id);

        return back();
    }

    public function show(MedicalCertificate $certificate): Response
    {
        $this->authorize('view', $certificate);

        $url = $this->storage->temporaryUrl($certificate->path, 5);

        return redirect()->away($url);
    }

    public function destroy(Team $team, TeamMember $member): RedirectResponse
    {
        $cert = $member->medicalCertificate;
        if (! $cert) {
            return back();
        }

        $this->authorize('delete', $cert);

        $cert->markSuperseded();
        $this->audit->log('certificate.removed', $cert);

        return back();
    }

    public function manualApprove(MedicalCertificate $certificate): RedirectResponse
    {
        $this->authorize('manualApprove', $certificate);

        $certificate->markValid(
            $certificate->expires_at ?? now()->addYear(),
            $certificate->extracted_name ?? 'Manuelno potvrđeno',
            $certificate->ocr_confidence ?? 1.0,
        );

        $this->audit->log('certificate.manual_approved', $certificate);

        return back();
    }
}
```

- [ ] **Step 4: Routes**

Append na `routes/teams.php`:

```php
use App\Http\Controllers\MedicalCertificateController;

Route::middleware(['auth', 'verified'])->group(function () {
    Route::post('teams/{team}/members/{member}/certificate', [MedicalCertificateController::class, 'store'])
        ->name('teams.members.certificate.store');
    Route::delete('teams/{team}/members/{member}/certificate', [MedicalCertificateController::class, 'destroy'])
        ->name('teams.members.certificate.destroy');
    Route::get('certificates/{certificate}', [MedicalCertificateController::class, 'show'])
        ->name('certificates.show');
});

Route::middleware(['auth', 'verified', 'role:admin'])->group(function () {
    Route::post('admin/certificates/{certificate}/manual-approve', [MedicalCertificateController::class, 'manualApprove'])
        ->name('admin.certificates.manual-approve');
});
```

- [ ] **Step 5: Run, PASS, commit**

```powershell
php artisan test --compact --filter=UploadTest
vendor/bin/pint --dirty --format agent
git add app/Http/Controllers/MedicalCertificateController.php app/Http/Requests/StoreMedicalCertificateRequest.php routes/teams.php tests/Feature/MedicalCertificate/
git commit -m "T2.1b: MedicalCertificateController upload + supersede + admin manual approve"
```

---

## Task 5: Frontend `UploadDropzone` + `CertificateStatusBadge`

**Files:**
- Create: `resources/js/components/medical-certificates/UploadDropzone.tsx`
- Create: `resources/js/components/medical-certificates/CertificateStatusBadge.tsx`
- Modify: `resources/js/components/teams/TeamMembersList.tsx` (integriši upload kontrolu)

- [ ] **Step 1: `CertificateStatusBadge.tsx`**

```tsx
// resources/js/components/medical-certificates/CertificateStatusBadge.tsx
import { Check, AlertTriangle, X, Eye, Clock, Archive } from 'lucide-react';

const config: Record<string, { label: string; color: string; icon: any }> = {
    pending: { label: 'OCR u toku', color: 'bg-yellow-100 text-yellow-800', icon: Clock },
    valid: { label: 'Validna', color: 'bg-green-100 text-green-800', icon: Check },
    expired: { label: 'Istekla', color: 'bg-red-100 text-red-800', icon: AlertTriangle },
    invalid: { label: 'Nevalidna', color: 'bg-red-100 text-red-800', icon: X },
    manual_review: { label: 'Ručna provjera', color: 'bg-purple-100 text-purple-800', icon: Eye },
    superseded: { label: 'Zamijenjena', color: 'bg-gray-100 text-gray-800', icon: Archive },
};

export function CertificateStatusBadge({ status }: { status: string }) {
    const c = config[status] ?? config.pending;
    const Icon = c.icon;
    return (
        <span className={`inline-flex items-center gap-1 text-xs px-2 py-0.5 rounded ${c.color}`}>
            <Icon className="h-3 w-3" /> {c.label}
        </span>
    );
}
```

- [ ] **Step 2: `UploadDropzone.tsx`**

```tsx
// resources/js/components/medical-certificates/UploadDropzone.tsx
import { router } from '@inertiajs/react';
import { useRef, useState } from 'react';
import { Upload } from 'lucide-react';

type Props = {
    teamId: number;
    memberId: number;
    onUploaded?: () => void;
};

export function UploadDropzone({ teamId, memberId, onUploaded }: Props) {
    const inputRef = useRef<HTMLInputElement>(null);
    const [uploading, setUploading] = useState(false);

    const handleFile = (file: File) => {
        if (file.size > 10 * 1024 * 1024) {
            alert('Fajl ne smije biti veći od 10 MB.');
            return;
        }
        setUploading(true);
        const data = new FormData();
        data.append('file', file);
        router.post(`/teams/${teamId}/members/${memberId}/certificate`, data, {
            preserveScroll: true,
            forceFormData: true,
            onFinish: () => {
                setUploading(false);
                onUploaded?.();
            },
        });
    };

    return (
        <button
            type="button"
            onClick={() => inputRef.current?.click()}
            disabled={uploading}
            className="inline-flex items-center gap-1 rounded border border-dashed px-3 py-1 text-xs hover:bg-muted"
        >
            <Upload className="h-3 w-3" />
            {uploading ? 'Šaljem...' : 'Upload potvrde'}
            <input
                ref={inputRef}
                type="file"
                accept="application/pdf,image/jpeg,image/png"
                className="hidden"
                onChange={(e) => e.target.files?.[0] && handleFile(e.target.files[0])}
            />
        </button>
    );
}
```

- [ ] **Step 3: Integriši u `TeamMembersList.tsx`**

> **Napomena:** Ovo je shared edit zona sa T2.1a. Koordiniši kroz import dodavanje, ne prepisivanje cijelog fajla.

Modify `resources/js/components/teams/TeamMembersList.tsx` — zamijeni `<span className="text-xs text-muted-foreground">Bez potvrde</span>` sa:

```tsx
{cert ? (
    <CertificateStatusBadge status={cert.status} />
) : canEdit && (
    <UploadDropzone teamId={teamId} memberId={m.id} />
)}
```

Dodaj imports:

```tsx
import { CertificateStatusBadge } from '@/components/medical-certificates/CertificateStatusBadge';
import { UploadDropzone } from '@/components/medical-certificates/UploadDropzone';
```

- [ ] **Step 4: Inertia partial reload za status update (kad job završi)**

Modify `resources/js/pages/teams/edit.tsx` da rade Inertia `router.reload` periodično ako neki cert ima `pending`:

```tsx
import { useEffect } from 'react';
import { router } from '@inertiajs/react';

// Unutar TeamsEdit komponente:
useEffect(() => {
    const hasPending = team.members.some((m: any) => m.medical_certificate?.status === 'pending');
    if (!hasPending) return;

    const interval = setInterval(() => {
        router.reload({ only: ['team'], preserveScroll: true });
    }, 2000);

    return () => clearInterval(interval);
}, [team]);
```

- [ ] **Step 5: Pint + commit**

```powershell
vendor/bin/pint --dirty --format agent
git add resources/js/components/medical-certificates/ resources/js/components/teams/TeamMembersList.tsx resources/js/pages/teams/edit.tsx
git commit -m "T2.1b: UploadDropzone + CertificateStatusBadge + partial reload polling"
```

---

## Task 6: `ExpireMedicalCertificatesJob` scheduled

**Files:**
- Create: `app/Jobs/ExpireMedicalCertificatesJob.php`
- Modify: `routes/console.php` (schedule)
- Test: `tests/Feature/Jobs/ExpireMedicalCertificatesJobTest.php`

- [ ] **Step 1: Failing test**

```php
<?php // tests/Feature/Jobs/ExpireMedicalCertificatesJobTest.php

use App\Enums\MedicalCertificateStatus;
use App\Jobs\ExpireMedicalCertificatesJob;
use App\Models\MedicalCertificate;

it('marks valid certs with past expires_at as expired', function () {
    $cert = MedicalCertificate::factory()->create([
        'status' => MedicalCertificateStatus::Valid,
        'expires_at' => now()->subDay(),
    ]);
    $stillValid = MedicalCertificate::factory()->create([
        'status' => MedicalCertificateStatus::Valid,
        'expires_at' => now()->addMonth(),
    ]);

    ExpireMedicalCertificatesJob::dispatchSync();

    expect($cert->fresh()->status)->toBe(MedicalCertificateStatus::Expired);
    expect($stillValid->fresh()->status)->toBe(MedicalCertificateStatus::Valid);
});
```

- [ ] **Step 2: Job**

```php
<?php // app/Jobs/ExpireMedicalCertificatesJob.php

namespace App\Jobs;

use App\Enums\MedicalCertificateStatus;
use App\Models\MedicalCertificate;
use App\Services\AuditLogger;
use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Foundation\Bus\Dispatchable;
use Illuminate\Queue\InteractsWithQueue;
use Illuminate\Queue\SerializesModels;

class ExpireMedicalCertificatesJob implements ShouldQueue
{
    use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;

    public function handle(AuditLogger $audit): void
    {
        $expired = MedicalCertificate::where('status', MedicalCertificateStatus::Valid)
            ->whereDate('expires_at', '<', now())
            ->get();

        foreach ($expired as $cert) {
            $cert->markExpired();
            $audit->log('certificate.expired_by_cron', $cert);
        }
    }
}
```

- [ ] **Step 3: Schedule**

Modify `routes/console.php`:

```php
<?php

use App\Jobs\ExpireMedicalCertificatesJob;
use Illuminate\Foundation\Inspiring;
use Illuminate\Support\Facades\Artisan;
use Illuminate\Support\Facades\Schedule;

Artisan::command('inspire', function () {
    $this->comment(Inspiring::quote());
})->purpose('Display an inspiring quote');

Schedule::job(new ExpireMedicalCertificatesJob)->dailyAt('02:00');
```

- [ ] **Step 4: Run, PASS, commit**

```powershell
php artisan test --compact --filter=ExpireMedicalCertificatesJobTest
vendor/bin/pint --dirty --format agent
git add app/Jobs/ExpireMedicalCertificatesJob.php routes/console.php tests/Feature/Jobs/ExpireMedicalCertificatesJobTest.php
git commit -m "T2.1b: ExpireMedicalCertificatesJob scheduled daily 02:00"
```

---

## Task 7: lang/me/medical-certificates

**Files:**
- Create: `lang/me/medical-certificates.php`

```php
<?php

return [
    'upload' => 'Upload ljekarske potvrde',
    'status.pending' => 'OCR u toku',
    'status.valid' => 'Validna',
    'status.expired' => 'Istekla',
    'status.invalid' => 'Nevalidna',
    'status.manual_review' => 'Ručna provjera',
    'status.superseded' => 'Zamijenjena',
    'too_large' => 'Fajl ne smije biti veći od 10 MB.',
    'invalid_format' => 'Dozvoljeni formati: PDF, JPG, PNG.',
    'expires_at' => 'Datum isteka',
    'extracted_name' => 'Ime sa potvrde',
    'confidence' => 'OCR pouzdanost',
    'manual_approve' => 'Manuelno potvrdi',
];
```

```powershell
git add lang/me/medical-certificates.php
git commit -m "T2.1b: lang/me/medical-certificates CG translations"
```

---

## Task 8: Final verify + PR

- [ ] **Step 1: Tests**

```powershell
php artisan test --compact
```

- [ ] **Step 2: Queue worker provjera (manualno)**

```powershell
composer run dev
```

U drugom shell-u, login kao profesor, uploaduj `petar_petrovic_2028-12-31.pdf` (može biti prazna PDF datoteka sa tim imenom). Pratiti queue:

```powershell
php artisan queue:work --once
```

Status mora preći iz `pending` u `valid`.

- [ ] **Step 3: Push i PR**

```powershell
git push -u origin feature/t2.1b-uc5-ocr
```

PR title: `[T2.1b] UC5 — OCR Pipeline (UC5, UC6)`.

---

## Acceptance criteria

- [x] Upload PDF prolazi, status `pending`, posle queue work-a `valid`
- [x] Filename sa "expired" datumom → status `expired`
- [x] Loš format → 422
- [x] Drugi profesor → 403
- [x] Replace cert markira stari kao `superseded`
- [x] State transitions enforced (valid → pending baca exception)
- [x] Cron job markira istekli cert

## NE radi

- Pravi Google Vision API (FakeOcr za sad)
- Manual review UI dashboard (T3.1 ili kasnije)
- Virus scan