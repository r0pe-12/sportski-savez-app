# T1.3 — Faza 1: Cross-cutting infrastruktura — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Phase:** 1 (paralelan sa T1.1, T1.2) · **Track ID:** T1.3 · **Procijenjeno:** 1 nedjelja
**UC pokriveni:** (cross-cutting)
**Spec referenca:** sekcije 9.4 (Queue), 9.5 (Notifications), 10.3 (Mock adapters), 11.5 (File storage), 13.3 (Audit log)
**Meta-plan:** [`specs/000-paralelni-plan.md`](000-paralelni-plan.md)
**Blokira:** sve Phase 2 · **Blokiran-od:** F2

**Goal:** Cross-cutting servisi spremni: `AuditLogger`, notification base, mock OCR + eDnevnik adapter-i, `PrivateFileStorage`. Bez UC implementacije — Phase 2 track-ovi konzumiraju ove alate.

**Architecture:** AuditLogger je singleton servis koji piše u `audit_log`. Notification matrica (spec 9.5) ide kroz Laravel `Notifications` (mail + database channel). FakeOcrAdapter implementira `OcrAdapter` interface i vraća valid/expired/invalid po filename konvenciji `{name}_{prezime}_{YYYY-MM-DD}.{ext}`. FakeEDnevnikAdapter koristi seed-ovane JMB-ove iz `StudentSeeder`. PrivateFileStorage je tanki wrapper oko `Storage::disk('local')` sa UUID path generation i signed URL-ovima.

**Tech Stack:** Laravel 13 sa database queue u dev. Nikakve nove dependency.

**Koordinacija:**
- T1.1 koristi `AuditLogger` (stub privremeno, prepiše se ovde).
- T1.2 koristi `AuditLogger`.
- T2.1b koristi `OcrAdapter` interface + `FakeOcrAdapter`.
- T2.2 koristi `EDnevnikAdapter` interface + `FakeEDnevnikAdapter`.
- T2.1b i T2.4 koriste `PrivateFileStorage`.

---

## Pre-flight

- [ ] **Provjeri F2 + worktree**

```powershell
git worktree add ../sportski-savez-app-t1.3 -b feature/t1.3-cross-cutting-infra
cd ../sportski-savez-app-t1.3
php artisan migrate
php artisan test --compact
```

---

## Task 1: `AuditLogger` servis (zamjenjuje T1.1 stub)

**Files:**
- Modify (ili Create): `app/Services/AuditLogger.php`
- Test: `tests/Unit/AuditLoggerTest.php`

- [ ] **Step 1: Failing test**

```php
<?php // tests/Unit/AuditLoggerTest.php

use App\Models\AuditLogEntry;
use App\Models\School;
use App\Models\User;
use App\Services\AuditLogger;

it('logs an entry with user, subject, payload', function () {
    $user = User::factory()->admin()->create();
    $school = School::factory()->create();

    $this->actingAs($user);

    $entry = app(AuditLogger::class)->log('school.created', $school, ['code' => $school->code]);

    expect($entry->action)->toBe('school.created');
    expect($entry->user_id)->toBe($user->id);
    expect($entry->subject_type)->toBe(School::class);
    expect($entry->subject_id)->toBe($school->id);
    expect($entry->payload)->toBe(['code' => $school->code]);
});

it('captures IP and user agent from request', function () {
    $entry = app(AuditLogger::class)->log('system.action');

    expect($entry->ip)->not->toBeNull();
});

it('logs without user when guest', function () {
    $entry = app(AuditLogger::class)->log('public.action');
    expect($entry->user_id)->toBeNull();
    expect($entry->action)->toBe('public.action');
});

it('logs without subject when omitted', function () {
    $entry = app(AuditLogger::class)->log('system.boot');
    expect($entry->subject_type)->toBeNull();
});
```

- [ ] **Step 2: Implementiraj `AuditLogger`**

```php
<?php // app/Services/AuditLogger.php

namespace App\Services;

use App\Models\AuditLogEntry;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Support\Facades\Request;

class AuditLogger
{
    /** @param array<string, mixed> $payload */
    public function log(string $action, ?Model $subject = null, array $payload = []): AuditLogEntry
    {
        return AuditLogEntry::create([
            'user_id' => auth()->id(),
            'action' => $action,
            'subject_type' => $subject?->getMorphClass(),
            'subject_id' => $subject?->getKey(),
            'payload' => $payload,
            'ip' => Request::ip(),
            'user_agent' => substr((string) Request::userAgent(), 0, 255),
            'created_at' => now(),
        ]);
    }
}
```

- [ ] **Step 3: Run, PASS, commit**

```powershell
php artisan test --compact --filter=AuditLoggerTest
vendor/bin/pint --dirty --format agent
git add app/Services/AuditLogger.php tests/Unit/AuditLoggerTest.php
git commit -m "T1.3: AuditLogger service with ip + user_agent capture"
```

---

## Task 2: `LogsAudit` trait (opcionalno auto-log za modele)

**Files:**
- Create: `app/Concerns/LogsAudit.php`
- Test: `tests/Feature/LogsAuditTraitTest.php`

> **Napomena:** Ovo je opciono — Controller-i mogu eksplicitno zvati `AuditLogger`. Trait je za modele koji žele auto-log saved/deleted bez Controller intervention.

- [ ] **Step 1: Failing test**

```php
<?php // tests/Feature/LogsAuditTraitTest.php

use App\Models\AuditLogEntry;
use App\Models\School;

it('auto-logs school save through trait', function () {
    // Pretpostavlja School trait LogsAudit
    $school = School::factory()->create();
    expect(AuditLogEntry::where('action', 'school.saved')->where('subject_id', $school->id)->exists())
        ->toBeTrue();
});
```

> **Odluka:** **NE radimo** trait u T1.3. Razlog: dva izvora truth-a (Controller eksplicitni log + trait auto log) → duplicirane zapisne. Phase 2 Controller-i će eksplicitno zvati `AuditLogger`. Skip ovaj task.

- [ ] **Step 2: Označi task kao N/A i nastavi**

```powershell
git rm tests/Feature/LogsAuditTraitTest.php 2>$null
```

---

## Task 3: `OcrAdapter` interface + `FakeOcrAdapter`

**Files:**
- Create: `app/Contracts/OcrAdapter.php`
- Create: `app/Adapters/Ocr/FakeOcrAdapter.php`
- Create: `app/Adapters/Ocr/Dto/OcrResult.php`
- Create: `config/ocr.php`
- Modify: `app/Providers/AppServiceProvider.php` (binding interface → fake)
- Test: `tests/Unit/Adapters/FakeOcrAdapterTest.php`

Po spec 10.3: file-name konvencija `{ime}_{prezime}_{YYYY-MM-DD}.{ext}`. Vraća validan dokument ili expired ako datum prošao.

- [ ] **Step 1: Failing test**

```php
<?php // tests/Unit/Adapters/FakeOcrAdapterTest.php

use App\Adapters\Ocr\Dto\OcrResult;
use App\Contracts\OcrAdapter;

beforeEach(fn () => $this->adapter = app(OcrAdapter::class));

it('extracts name and dates from filename convention', function () {
    $result = $this->adapter->extract('storage/dummy', 'petar_petrovic_2027-06-15.pdf');

    expect($result)->toBeInstanceOf(OcrResult::class);
    expect($result->extractedName)->toBe('Petar Petrović');
    expect($result->expiresAt->toDateString())->toBe('2027-06-15');
    expect($result->confidence)->toBeGreaterThan(0.8);
});

it('returns expired when filename date is in past', function () {
    $result = $this->adapter->extract('storage/dummy', 'ana_anic_2020-01-01.pdf');
    expect($result->isExpired())->toBeTrue();
});

it('returns low confidence (manual review) for invalid filename', function () {
    $result = $this->adapter->extract('storage/dummy', 'random_garbage.pdf');
    expect($result->confidence)->toBeLessThan(0.5);
    expect($result->needsManualReview())->toBeTrue();
});

it('handles non-PDF MIME', function () {
    $result = $this->adapter->extract('storage/dummy', 'marko_markovic_2027-09-01.jpg');
    expect($result->extractedName)->toBe('Marko Marković');
});
```

- [ ] **Step 2: Interface**

```php
<?php // app/Contracts/OcrAdapter.php

namespace App\Contracts;

use App\Adapters\Ocr\Dto\OcrResult;

interface OcrAdapter
{
    /**
     * Ekstrahuje podatke iz dokumenta.
     *
     * @param  string  $path  Path u storage disk-u.
     * @param  string  $originalFilename  Ime fajla koje korisnik uploadovao (može sadržati hint za fake adapter).
     */
    public function extract(string $path, string $originalFilename): OcrResult;
}
```

- [ ] **Step 3: DTO**

```php
<?php // app/Adapters/Ocr/Dto/OcrResult.php

namespace App\Adapters\Ocr\Dto;

use Carbon\Carbon;

readonly class OcrResult
{
    public function __construct(
        public ?string $extractedName,
        public ?Carbon $issuedAt,
        public ?Carbon $expiresAt,
        public float $confidence,         // 0.0 — 1.0
        public string $rawResponse = '',
    ) {}

    public function isExpired(): bool
    {
        return $this->expiresAt?->isPast() ?? false;
    }

    public function needsManualReview(): bool
    {
        return $this->confidence < (float) config('ocr.manual_review_threshold', 0.6);
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        return [
            'extracted_name' => $this->extractedName,
            'issued_at' => $this->issuedAt?->toDateString(),
            'expires_at' => $this->expiresAt?->toDateString(),
            'confidence' => $this->confidence,
        ];
    }
}
```

- [ ] **Step 4: FakeOcrAdapter**

```php
<?php // app/Adapters/Ocr/FakeOcrAdapter.php

namespace App\Adapters\Ocr;

use App\Adapters\Ocr\Dto\OcrResult;
use App\Contracts\OcrAdapter;
use Carbon\Carbon;
use Illuminate\Support\Str;

class FakeOcrAdapter implements OcrAdapter
{
    /**
     * File-name konvencija: {ime}_{prezime}_{YYYY-MM-DD}.{ext}
     * - ime/prezime dobijemo Title Case-om
     * - YYYY-MM-DD je expires_at
     * - issued_at = expires_at - 1 godina
     * - Ako filename ne matchuje, vraćamo low confidence (manual review)
     */
    public function extract(string $path, string $originalFilename): OcrResult
    {
        $stem = pathinfo($originalFilename, PATHINFO_FILENAME);

        if (! preg_match('/^([a-zA-ZčČćĆđĐšŠžŽ]+)_([a-zA-ZčČćĆđĐšŠžŽ]+)_(\d{4}-\d{2}-\d{2})$/u', $stem, $m)) {
            return new OcrResult(
                extractedName: null,
                issuedAt: null,
                expiresAt: null,
                confidence: 0.35,
                rawResponse: 'FAKE_NO_MATCH',
            );
        }

        $name = Str::title($m[1]).' '.Str::title($m[2]);
        $expires = Carbon::createFromFormat('Y-m-d', $m[3])->startOfDay();
        $issued = (clone $expires)->subYear();

        return new OcrResult(
            extractedName: $name,
            issuedAt: $issued,
            expiresAt: $expires,
            confidence: 0.95,
            rawResponse: 'FAKE_MATCH',
        );
    }
}
```

- [ ] **Step 5: config/ocr.php**

```php
<?php

return [
    'adapter' => env('OCR_ADAPTER', 'fake'),
    'manual_review_threshold' => 0.6,
    'languages' => ['sr', 'bs', 'hr'],
];
```

- [ ] **Step 6: Binding u AppServiceProvider**

```php
// app/Providers/AppServiceProvider.php — register()

public function register(): void
{
    $this->app->bind(\App\Contracts\OcrAdapter::class, function () {
        return config('ocr.adapter') === 'fake'
            ? new \App\Adapters\Ocr\FakeOcrAdapter()
            : throw new \LogicException('Real OcrAdapter not implemented yet. Set OCR_ADAPTER=fake.');
    });
}
```

- [ ] **Step 7: Run, PASS, commit**

```powershell
php artisan test --compact --filter=FakeOcrAdapterTest
vendor/bin/pint --dirty --format agent
git add app/Contracts/OcrAdapter.php app/Adapters/Ocr/ config/ocr.php app/Providers/AppServiceProvider.php tests/Unit/Adapters/FakeOcrAdapterTest.php
git commit -m "T1.3: OcrAdapter contract + FakeOcrAdapter with filename convention"
```

---

## Task 4: `EDnevnikAdapter` interface + `FakeEDnevnikAdapter`

**Files:**
- Create: `app/Contracts/EDnevnikAdapter.php`
- Create: `app/Adapters/EDnevnik/FakeEDnevnikAdapter.php`
- Create: `app/Adapters/EDnevnik/Dto/EDnevnikStudentDto.php`
- Create: `app/Adapters/EDnevnik/Exceptions/EDnevnikNotFoundException.php`
- Create: `config/ednevnik.php`
- Test: `tests/Unit/Adapters/FakeEDnevnikAdapterTest.php`

Po spec 5.4 i 11.1.

- [ ] **Step 1: Failing test**

```php
<?php // tests/Unit/Adapters/FakeEDnevnikAdapterTest.php

use App\Adapters\EDnevnik\Dto\EDnevnikStudentDto;
use App\Adapters\EDnevnik\Exceptions\EDnevnikNotFoundException;
use App\Contracts\EDnevnikAdapter;
use App\Models\Student;

beforeEach(fn () => $this->adapter = app(EDnevnikAdapter::class));

it('returns verified data for seeded JMB', function () {
    $student = Student::factory()->create(['jmb' => '0101010250001']);
    $dto = $this->adapter->fetchStudentByJmb('0101010250001');

    expect($dto)->toBeInstanceOf(EDnevnikStudentDto::class);
    expect($dto->jmb)->toBe('0101010250001');
    expect($dto->redovan)->toBeTrue();
});

it('throws NotFound for unknown JMB', function () {
    expect(fn () => $this->adapter->fetchStudentByJmb('9999999999999'))
        ->toThrow(EDnevnikNotFoundException::class);
});

it('returns mismatched data for specific test JMB pattern', function () {
    // Konvencija: JMB koji počinje sa "00000" → mismatched grade
    $student = Student::factory()->create(['jmb' => '0000000000001', 'grade' => '8-2']);
    $dto = $this->adapter->fetchStudentByJmb('0000000000001');

    expect($dto->razred)->not->toBe('8-2'); // mismatched
});

it('returns data even when student exists locally with different data (mismatch testing)', function () {
    $student = Student::factory()->create([
        'jmb' => '0101010250002',
        'name' => 'Lokalno Ime',
    ]);
    $dto = $this->adapter->fetchStudentByJmb('0101010250002');

    expect($dto->ime.' '.$dto->prezime)->toBe('Lokalno Ime');
});
```

- [ ] **Step 2: Interface**

```php
<?php // app/Contracts/EDnevnikAdapter.php

namespace App\Contracts;

use App\Adapters\EDnevnik\Dto\EDnevnikStudentDto;

interface EDnevnikAdapter
{
    /**
     * Dohvat učenika iz eDnevnik-a po JMB-u.
     *
     * @throws \App\Adapters\EDnevnik\Exceptions\EDnevnikNotFoundException
     * @throws \App\Adapters\EDnevnik\Exceptions\EDnevnikUnavailableException
     */
    public function fetchStudentByJmb(string $jmb): EDnevnikStudentDto;
}
```

- [ ] **Step 3: DTO i Exceptions**

```php
<?php // app/Adapters/EDnevnik/Dto/EDnevnikStudentDto.php

namespace App\Adapters\EDnevnik\Dto;

use Carbon\Carbon;

readonly class EDnevnikStudentDto
{
    public function __construct(
        public string $jmb,
        public string $ime,
        public string $prezime,
        public string $sifraSkole,
        public string $razred,
        public bool $redovan,
        public Carbon $datumZadnjegStatusa,
    ) {}
}
```

```php
<?php // app/Adapters/EDnevnik/Exceptions/EDnevnikNotFoundException.php

namespace App\Adapters\EDnevnik\Exceptions;

class EDnevnikNotFoundException extends \RuntimeException {}
```

```php
<?php // app/Adapters/EDnevnik/Exceptions/EDnevnikUnavailableException.php

namespace App\Adapters\EDnevnik\Exceptions;

class EDnevnikUnavailableException extends \RuntimeException {}
```

- [ ] **Step 4: FakeEDnevnikAdapter**

```php
<?php // app/Adapters/EDnevnik/FakeEDnevnikAdapter.php

namespace App\Adapters\EDnevnik;

use App\Adapters\EDnevnik\Dto\EDnevnikStudentDto;
use App\Adapters\EDnevnik\Exceptions\EDnevnikNotFoundException;
use App\Contracts\EDnevnikAdapter;
use App\Models\Student;
use Carbon\Carbon;
use Illuminate\Support\Str;

class FakeEDnevnikAdapter implements EDnevnikAdapter
{
    /**
     * Determinističko ponašanje:
     * - JMB koji počinje sa "00000" → vraća mismatched podatke (razred izmijenjen)
     * - JMB koji počinje sa "99999" → throw NotFound
     * - Inače: vraća podatke koje matchuju lokalnu bazu (verified case)
     */
    public function fetchStudentByJmb(string $jmb): EDnevnikStudentDto
    {
        if (Str::startsWith($jmb, '99999')) {
            throw new EDnevnikNotFoundException("Student with JMB {$jmb} not found in eDnevnik.");
        }

        $student = Student::withoutGlobalScope('student')
            ->where('jmb', $jmb)
            ->first();

        if (! $student) {
            throw new EDnevnikNotFoundException("Student with JMB {$jmb} not found in local DB (for fake adapter).");
        }

        $parts = explode(' ', $student->name, 2);
        $ime = $parts[0] ?? '';
        $prezime = $parts[1] ?? '';

        // Mismatch case: izmijeni razred
        $razred = $student->grade ?? '7-1';
        if (Str::startsWith($jmb, '00000')) {
            $razred = '9-9'; // garantovan mismatch
        }

        return new EDnevnikStudentDto(
            jmb: $jmb,
            ime: $ime,
            prezime: $prezime,
            sifraSkole: $student->school?->code ?? 'OS-UNKNOWN',
            razred: $razred,
            redovan: true,
            datumZadnjegStatusa: Carbon::now()->subDays(7),
        );
    }
}
```

- [ ] **Step 5: config/ednevnik.php**

```php
<?php

return [
    'adapter' => env('EDNEVNIK_ADAPTER', 'fake'),
    'timeout_seconds' => 10,
    'max_retries' => 3,
];
```

- [ ] **Step 6: Binding u AppServiceProvider**

```php
$this->app->bind(\App\Contracts\EDnevnikAdapter::class, function () {
    return config('ednevnik.adapter') === 'fake'
        ? new \App\Adapters\EDnevnik\FakeEDnevnikAdapter()
        : throw new \LogicException('Real EDnevnikAdapter not implemented yet. Set EDNEVNIK_ADAPTER=fake.');
});
```

- [ ] **Step 7: Run, PASS, commit**

```powershell
php artisan test --compact --filter=FakeEDnevnikAdapterTest
vendor/bin/pint --dirty --format agent
git add app/Contracts/EDnevnikAdapter.php app/Adapters/EDnevnik/ config/ednevnik.php app/Providers/AppServiceProvider.php tests/Unit/Adapters/FakeEDnevnikAdapterTest.php
git commit -m "T1.3: EDnevnikAdapter contract + FakeEDnevnikAdapter with deterministic JMB rules"
```

---

## Task 5: `PrivateFileStorage` servis

**Files:**
- Create: `app/Services/PrivateFileStorage.php`
- Create: `config/filesystems.php` modification (private disk)
- Test: `tests/Unit/PrivateFileStorageTest.php`

Po spec 11.5: `students/{student_id}/photos/{uuid}.{ext}`, `medical-certificates/{team_member_id}/{uuid}.{ext}`.

- [ ] **Step 1: Provjeri `private` disk u `config/filesystems.php`**

```powershell
Get-Content config/filesystems.php | Select-String "private"
```

Ako ne postoji, doda u `config/filesystems.php`:

```php
'disks' => [
    // ... postojeći diskovi
    'private' => [
        'driver' => 'local',
        'root' => storage_path('app/private'),
        'serve' => true,
        'throw' => false,
        'visibility' => 'private',
    ],
],
```

- [ ] **Step 2: Failing test**

```php
<?php // tests/Unit/PrivateFileStorageTest.php

use App\Models\Student;
use App\Services\PrivateFileStorage;
use Illuminate\Http\UploadedFile;
use Illuminate\Support\Facades\Storage;

beforeEach(fn () => Storage::fake('private'));

it('stores file with UUID-based path scoped to model', function () {
    $student = Student::factory()->create();
    $file = UploadedFile::fake()->image('avatar.jpg');

    $path = app(PrivateFileStorage::class)->storeFor($student, $file, 'photos');

    expect($path)->toStartWith("students/{$student->id}/photos/");
    expect($path)->toEndWith('.jpg');
    Storage::disk('private')->assertExists($path);
});

it('generates UUID v4 in the filename', function () {
    $student = Student::factory()->create();
    $file = UploadedFile::fake()->image('x.png');
    $path = app(PrivateFileStorage::class)->storeFor($student, $file, 'photos');

    expect($path)->toMatch('/[0-9a-f-]{36}\.png$/');
});

it('deleteFor removes all files for owner directory', function () {
    $student = Student::factory()->create();
    $storage = app(PrivateFileStorage::class);
    $storage->storeFor($student, UploadedFile::fake()->image('1.jpg'), 'photos');
    $storage->storeFor($student, UploadedFile::fake()->image('2.jpg'), 'photos');

    $storage->deleteFor($student);

    expect(Storage::disk('private')->allFiles("students/{$student->id}"))->toBeEmpty();
});

it('temporaryUrl returns signed URL', function () {
    $student = Student::factory()->create();
    $path = app(PrivateFileStorage::class)->storeFor($student, UploadedFile::fake()->image('x.jpg'), 'photos');

    $url = app(PrivateFileStorage::class)->temporaryUrl($path, 5);
    expect($url)->toContain($path);
});
```

- [ ] **Step 3: Implementiraj `PrivateFileStorage`**

```php
<?php // app/Services/PrivateFileStorage.php

namespace App\Services;

use App\Models\MedicalCertificate;
use App\Models\Student;
use App\Models\TeamMember;
use App\Models\User;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Http\UploadedFile;
use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Str;

class PrivateFileStorage
{
    private const DISK = 'private';

    public function storeFor(Model $owner, UploadedFile $file, string $purpose): string
    {
        $uuid = (string) Str::uuid();
        $ext = $file->getClientOriginalExtension() ?: 'bin';
        $directory = $this->directoryFor($owner, $purpose);

        $relative = "{$directory}/{$uuid}.{$ext}";

        Storage::disk(self::DISK)->putFileAs($directory, $file, "{$uuid}.{$ext}");

        return $relative;
    }

    public function temporaryUrl(string $path, int $minutes = 5): string
    {
        $disk = Storage::disk(self::DISK);

        if (method_exists($disk, 'temporaryUrl')) {
            try {
                return $disk->temporaryUrl($path, now()->addMinutes($minutes));
            } catch (\Throwable) {
                // Local disk u dev možda ne podržava — pad na .url()
            }
        }

        return $disk->url($path);
    }

    public function deleteFor(Model $owner): void
    {
        $base = $this->ownerBaseDir($owner);
        if ($base) {
            Storage::disk(self::DISK)->deleteDirectory($base);
        }
    }

    private function directoryFor(Model $owner, string $purpose): string
    {
        $base = $this->ownerBaseDir($owner) ?? class_basename($owner).'/'.$owner->getKey();

        return "{$base}/{$purpose}";
    }

    private function ownerBaseDir(Model $owner): ?string
    {
        return match (true) {
            $owner instanceof Student => "students/{$owner->id}",
            $owner instanceof TeamMember => "medical-certificates/{$owner->id}",
            $owner instanceof MedicalCertificate => "medical-certificates/{$owner->team_member_id}",
            $owner instanceof User => "users/{$owner->id}",
            default => null,
        };
    }
}
```

- [ ] **Step 4: Run, PASS, commit**

```powershell
php artisan test --compact --filter=PrivateFileStorageTest
vendor/bin/pint --dirty --format agent
git add app/Services/PrivateFileStorage.php config/filesystems.php tests/Unit/PrivateFileStorageTest.php
git commit -m "T1.3: PrivateFileStorage service with UUID paths and per-owner directories"
```

---

## Task 6: Notification base + Inertia share unread count

**Files:**
- Provjeri da `notifications` tabela postoji (Laravel default — `php artisan notifications:table`)
- Modify: `app/Http/Middleware/HandleInertiaRequests.php` (već u T1.1 ali ovde verifikujemo)
- Create: `resources/js/components/notifications/NotificationBell.tsx`
- Test: `tests/Feature/NotificationShareTest.php`

> **Napomena:** Konkretne `Notification` klase (TeamSubmittedNotification, MedicalCertificateInvalidNotification itd.) idu u Phase 2 track-ovi koji ih trigger-uju. T1.3 daje samo infrastrukturu.

- [ ] **Step 1: Provjeri `notifications` tabelu**

```powershell
Get-ChildItem database/migrations/*notification* 2>$null
```

Ako ne postoji:
```powershell
php artisan notifications:table
php artisan migrate
```

- [ ] **Step 2: Failing test**

```php
<?php // tests/Feature/NotificationShareTest.php

use App\Models\Professor;
use Illuminate\Notifications\DatabaseNotification;

it('shares notification count to Inertia', function () {
    $prof = Professor::factory()->create();

    DatabaseNotification::create([
        'id' => (string) \Illuminate\Support\Str::uuid(),
        'type' => 'TestNotification',
        'notifiable_type' => $prof->getMorphClass(),
        'notifiable_id' => $prof->id,
        'data' => ['message' => 'test'],
    ]);

    $response = $this->actingAs($prof)->get('/dashboard');
    $page = $response->viewData('page');

    expect($page['props']['notifications']['unread_count'])->toBe(1);
});

it('marks notification as read endpoint', function () {
    $prof = Professor::factory()->create();
    $id = (string) \Illuminate\Support\Str::uuid();

    DatabaseNotification::create([
        'id' => $id, 'type' => 'X',
        'notifiable_type' => $prof->getMorphClass(),
        'notifiable_id' => $prof->id,
        'data' => [],
    ]);

    $this->actingAs($prof)->post("/notifications/{$id}/read");

    $notification = DatabaseNotification::find($id);
    expect($notification->read_at)->not->toBeNull();
});
```

- [ ] **Step 3: NotificationController za mark-as-read**

```php
<?php // app/Http/Controllers/NotificationController.php

namespace App\Http\Controllers;

use Illuminate\Http\RedirectResponse;
use Illuminate\Notifications\DatabaseNotification;

class NotificationController extends Controller
{
    public function markAsRead(string $id): RedirectResponse
    {
        $notification = DatabaseNotification::where('id', $id)
            ->where('notifiable_id', auth()->id())
            ->firstOrFail();

        $notification->markAsRead();

        return back();
    }
}
```

- [ ] **Step 4: Dodaj rutu u `routes/public.php` ili novi `routes/notifications.php`**

Pošto Notifications nije eksplicitno u meta-plan 4.2 split-u, koristimo `routes/public.php` (univerzalno) sa auth middleware:

Append na kraj `routes/public.php`:

```php
use App\Http\Controllers\NotificationController;

Route::middleware(['auth', 'verified'])->group(function () {
    Route::post('/notifications/{id}/read', [NotificationController::class, 'markAsRead'])->name('notifications.read');
});
```

- [ ] **Step 5: `NotificationBell.tsx` placeholder komponenta**

```tsx
// resources/js/components/notifications/NotificationBell.tsx
import { usePage } from '@inertiajs/react';
import { Bell } from 'lucide-react';
import type { SharedData } from '@/types';

export function NotificationBell() {
    const { notifications } = usePage<SharedData>().props;
    const count = notifications.unread_count;

    return (
        <button className="relative inline-flex h-9 w-9 items-center justify-center rounded-md hover:bg-muted">
            <Bell className="h-5 w-5" />
            {count > 0 && (
                <span className="absolute -top-1 -right-1 inline-flex h-5 min-w-[1.25rem] items-center justify-center rounded-full bg-destructive px-1 text-xs font-medium text-white">
                    {count}
                </span>
            )}
        </button>
    );
}
```

> **Napomena:** integracija u layout-u je T1.1 obaveza (header treba uključiti `<NotificationBell />`). T1.3 daje komponentu.

- [ ] **Step 6: Run, PASS, commit**

```powershell
php artisan test --compact --filter=NotificationShare
vendor/bin/pint --dirty --format agent
git add app/Http/Controllers/NotificationController.php routes/public.php resources/js/components/notifications/ tests/Feature/NotificationShareTest.php
git commit -m "T1.3: notification infra — mark-as-read route + NotificationBell + Inertia share"
```

---

## Task 7: AuditLogEntry policy (immutable enforcement)

**Files:**
- Create: `app/Policies/AuditLogPolicy.php`
- Test: dio `tests/Feature/Policies/AuditLogPolicyTest.php`

Po spec 13.4: admin only read; **niko** nema update/delete.

- [ ] **Step 1: Failing test**

```php
<?php // tests/Feature/Policies/AuditLogPolicyTest.php

use App\Models\AuditLogEntry;
use App\Models\Professor;
use App\Models\User;

it('admin can view audit log', function () {
    $admin = User::factory()->admin()->create();
    expect($admin->can('viewAny', AuditLogEntry::class))->toBeTrue();
});

it('professor cannot view audit log', function () {
    $prof = Professor::factory()->create();
    expect($prof->can('viewAny', AuditLogEntry::class))->toBeFalse();
});

it('nobody can update or delete audit log entry', function () {
    $admin = User::factory()->admin()->create();
    $entry = AuditLogEntry::create(['action' => 'x', 'created_at' => now()]);

    expect($admin->can('update', $entry))->toBeFalse();
    expect($admin->can('delete', $entry))->toBeFalse();
});
```

- [ ] **Step 2: Policy**

```php
<?php // app/Policies/AuditLogPolicy.php

namespace App\Policies;

use App\Models\AuditLogEntry;
use App\Models\User;

class AuditLogPolicy
{
    public function viewAny(User $user): bool
    {
        return $user->isAdmin();
    }

    public function view(User $user, AuditLogEntry $entry): bool
    {
        return $user->isAdmin();
    }

    public function update(User $user, AuditLogEntry $entry): bool
    {
        return false;
    }

    public function delete(User $user, AuditLogEntry $entry): bool
    {
        return false;
    }
}
```

- [ ] **Step 3: Run, PASS, commit**

```powershell
php artisan test --compact --filter=AuditLogPolicyTest
vendor/bin/pint --dirty --format agent
git add app/Policies/AuditLogPolicy.php tests/Feature/Policies/AuditLogPolicyTest.php
git commit -m "T1.3: AuditLogPolicy — admin read, nobody update/delete"
```

---

## Task 8: Queue connection config

**Files:**
- Modify: `config/queue.php` (provjeri da `database` driver postoji za naše queue imena — već default)
- Update: `.env.example` (ako varijable iz F1 nisu pokupljene)

Po spec 9.4: `ocr`, `ednevnik`, `notifications` queue imena.

- [ ] **Step 1: Provjeri queue u `config/queue.php`**

```powershell
Get-Content config/queue.php
```

Default Laravel ima `database` connection. Naša queue imena (`ocr`, `ednevnik`, `notifications`) se dispatch-uju kroz `->onQueue('ocr')`. Ne treba poseban config za queue *imena* — Laravel ih kreira na lijevu po potrebi.

- [ ] **Step 2: Provjeri `composer run dev` script i dodaj queue worker po imenu (opcionalno)**

> **Odluka:** trenutni `dev` script ima `php artisan queue:listen --tries=1`. To je dovoljan default — listener će procesirati sve queue-ove. U produkciji ćemo imati per-queue worker proces, ali u dev je single listener OK.

Bez izmjena composer.json.

- [ ] **Step 3: ExpireMedicalCertificatesJob scheduled (skeleton)**

Po spec 9.4: scheduled daily 02:00, sve `valid` certifikate sa `expires_at < today` prelaze u `expired`.

> **Napomena:** Sam job ide u T2.1b (OCR pipeline). T1.3 samo dokumentuje da `routes/console.php` će biti gdje se schedule registruje.

Provjeri `routes/console.php`:

```powershell
Get-Content routes/console.php
```

OK — T2.1b će dodati schedule entry tu.

- [ ] **Step 4: Commit (možda nema novih fajlova)**

```powershell
git status
# Ako nema izmjena, skoči
```

---

## Task 9: Final verification + wayfinder

- [ ] **Step 1: Full test suita**

```powershell
php artisan test --compact
```
Expected: AuditLoggerTest, FakeOcrAdapterTest, FakeEDnevnikAdapterTest, PrivateFileStorageTest, NotificationShareTest, AuditLogPolicyTest sve PASS, plus svi prethodni.

- [ ] **Step 2: Build**

```powershell
npm run build
```
Expected: bez TS errora (`NotificationBell` komponenta type-checked).

- [ ] **Step 3: Tinker provjera fake adapter-a**

```powershell
php artisan tinker --execute 'echo app(App\Contracts\OcrAdapter::class)::class;'
```
Expected: `App\Adapters\Ocr\FakeOcrAdapter`.

```powershell
php artisan tinker --execute '$dto = app(App\Contracts\EDnevnikAdapter::class)->fetchStudentByJmb("0101010250001"); echo $dto->ime.PHP_EOL;'
```
Expected: ime učenika seedovanog sa JMB-om 0101010250001 (zahtjeva da je StudentSeeder pokrenut).

- [ ] **Step 4: Push i PR**

```powershell
git push -u origin feature/t1.3-cross-cutting-infra
```

PR title: `[T1.3] Cross-cutting infrastructure (AuditLogger, FakeAdapters, FileStorage)`.

---

## Acceptance criteria

- [x] Sve Pest unit testove pass
- [x] Pint clean
- [x] AuditLogger fake call u tinker piše zapis
- [x] FakeOcr i FakeEDnevnik vraćaju deterministic odgovore
- [x] PrivateFileStorage upload + download radi end-to-end
- [x] `routes/web.php` split kompletan (kreirano u F1, verifikovano ovdje)

## NE radi

- Audit log UI (T3.1)
- Phase 2 UC implementacije
- Pravi Google Vision / eDnevnik HTTP adapter-e
- Notification preferences UI
- Generic event bus

## TODO zaključeni

- ✓ Fake OCR konvencija: `{ime}_{prezime}_{YYYY-MM-DD}.{ext}`
- ✓ Audit log indexes (po user, action, datum) — postavljeni u F2 migraciji
- ✓ Notification fan-out: ide kroz `Notification::send()` sa `mail + database` channel — Phase 2 track-ovi koriste

## Self-review

- Spec 13.3 (Audit log model) — AuditLogger servis + Policy ✓
- Spec 9.4 (Queue) — queue connections konfigurisane ✓
- Spec 9.5 (Notifications) — base infrastruktura (mark-as-read, NotificationBell, unread count share) ✓
- Spec 10.3 (Mock adapters) — Fake* implementacije ✓
- Spec 11.5 (File storage) — PrivateFileStorage sa UUID paths ✓
- Meta-plan 4.1 (shared edit zones) — AppServiceProvider binding kontrolisano, ne dira drugu sekciju ✓