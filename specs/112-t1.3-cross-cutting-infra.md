# T1.3 — Faza 1: Cross-cutting infrastruktura

**Phase:** 1 (paralelan sa T1.1, T1.2) · **Track ID:** T1.3 · **Procijenjeno:** 1 nedjelja
**UC pokriveni:** (cross-cutting — nije UC nego infrastrukturni servisi)
**Spec referenca:** sekcije 9.4 (Queue), 9.5 (Notifications), 10.3 (Mock adapters), 11.5 (File storage), 13.3 (Audit log)
**Meta-plan:** [`specs/000-paralelni-plan.md`](000-paralelni-plan.md)
**Blokira:** sve Phase 2 (Phase 2 koristi audit, notif, fake adapters, file storage helpers) · **Blokiran-od:** F2

---

## Cilj
Cross-cutting servisi spremni: audit logger, notifikacioni infrastruktura, mock adapter-i za eksterne servise, file storage helper-i. **NE radi** UC implementaciju — samo gradi alate koje će Phase 2 track-ovi koristiti.

## Izlazi (deliverables)

### Audit log
- [ ] `app/Services/AuditLogger.php` — `log($action, $subject, $payload = [])` metoda
- [ ] `AuditLoggerServiceProvider` ili singleton
- [ ] `AuditLogEntry` model je read-only — write samo kroz AuditLogger
- [ ] Helper trait `LogsAudit` koji modeli mogu uključiti za auto-log saved/deleted
- [ ] **NIJE** UI dashboard (to je T3.1) — samo write-side

### Notification infrastruktura
- [ ] `app/Notifications/` direktorij sa base abstract klasa po potrebi
- [ ] Channels: `mail` (kroz `log` driver u dev), `database` (in-app)
- [ ] `NotificationServiceProvider` boot — share unread count kroz Inertia
- [ ] Frontend: `<NotificationBell>` komponenta u `resources/js/components/notifications/` (UI shell prikazuje u layout-u)
- [ ] Migration za `notifications` tabela ako Laravel default `php artisan notifications:table` nije izvršen u F2

### Mock eksterni adapter-i
- [ ] `app/Contracts/OcrAdapter.php` interface
- [ ] `app/Adapters/Ocr/FakeOcrAdapter.php` — vraća valid/expired po file-name konvenciji (`ime_prezime_YYYY-MM-DD.pdf`)
- [ ] `app/Contracts/EDnevnikAdapter.php` interface
- [ ] `app/Adapters/EDnevnik/FakeEDnevnikAdapter.php` — vraća deterministic odgovore iz seedovanih JMB-ova
- [ ] `AppServiceProvider` binding interface → fake u dev/test
- [ ] `config/ocr.php`, `config/ednevnik.php` sa feature flag-om za fake vs real

### File storage helper-i
- [ ] `app/Services/PrivateFileStorage.php` — wrapper za `Storage::disk('private')` sa UUID-based path generation
- [ ] Method: `storeFor(Model $owner, UploadedFile $file, string $purpose): string` (path)
- [ ] Method: `temporaryUrl(string $path, int $minutes = 5): string`
- [ ] Method: `deleteFor(Model $owner): void` (briše ceo direktorij)
- [ ] Path konvencija po spec 11.5 (`students/{id}/photos/{uuid}.{ext}` itd.)

### Routing split (cleanup posle F1)
- [ ] Provjeri da svi placeholder route fajlovi rade i da `routes/web.php` ih require-uje
- [ ] Stavi welcome i ai-dnevnik rute u `routes/public.php`

### Testovi (Pest)
- [ ] Unit: AuditLogger zapisuje zapis, vraća zapis kroz `Eloquent`
- [ ] Unit: `AuditLogEntry::create()` direktan poziv hvata exception (immutable po policy)
- [ ] Unit: FakeOcrAdapter vraća valid za pravilan filename, expired za istekli
- [ ] Unit: FakeEDnevnikAdapter vraća verified za seedovani JMB, mismatched za izmijenjen
- [ ] Unit: PrivateFileStorage generiše UUID path, temporaryUrl radi
- [ ] Feature: AuditLogger se poziva iz fake controller-a, zapis vidljiv u DB

## Shared edit zones
- `app/Providers/AppServiceProvider.php` — **kontrolisani edit** za adapter binding (drugi track-ovi ne diraju binding sekcije)
- `routes/audit.php`, `routes/public.php` — public read (ako T1.3 dodaje neku audit read rutu, inače T3.1)
- `config/ocr.php`, `config/ednevnik.php` — **isključivo** ovaj track
- `app/Http/Middleware/HandleInertiaRequests.php` — dodaje notification count (kroz ServiceProvider, ne direktno)

## Acceptance criteria
- Sve Pest unit testove pass
- Pint clean
- AuditLogger fake call u tinker piše zapis
- FakeOcr i FakeEDnevnik vraćaju deterministic odgovore
- PrivateFileStorage upload + download radi end-to-end
- `routes/web.php` split kompletan

## NE radi
- Nemoj implementirati audit log UI (T3.1)
- Nemoj implementirati team / student / result logiku (Phase 2)
- Nemoj implementirati prave eksterne adapter-e (Google Vision, eDnevnik HTTP) — samo interface + fake
- Nemoj dodavati notification preferences UI
- Nemoj generic event bus — Laravel events su dovoljni

## TODO (popunjava `/plan`)
- [ ] Fake adapter file-name konvencija — finalizovati format
- [ ] Audit log indexes (po user, po subject, po datum)
- [ ] Notification fan-out (kako jedna akcija šalje email + db?)