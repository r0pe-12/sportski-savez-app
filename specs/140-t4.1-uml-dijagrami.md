# T4.1 — Faza 4: UML dijagrami (5 vrsta) — Placeholder

> **Status:** Placeholder. Stvarni plan se generiše kroz `/plan` ili `superpowers:writing-plans` skill nakon merge-a T3.2.

**Phase:** 4 (paralelno sa T4.2, prije T4.3) · **Track ID:** T4.1 · **Procijenjeno:** 2 dana
**UC pokriveni:** (cross — dokumentacijski artefakt)
**Spec referenca:** §7 (Domain model), §9 (Arhitektura), §10 (Stack)
**Blokira:** T4.3 (konsolidovan izvještaj reference UML)
**Blokiran-od:** T3.2 (cijela aplikacija mora biti implementirana, jer UML se generiše iz koda)

---

## Goal

Pet UML dijagrama generisanih iz **implementiranog** koda (ne iz spec teksta), spremljenih kao PlantUML izvor + render PNG/SVG u `docs/zavrsni-izvjestaj/uml/`. Pokriva eksplicitni zahtjev profesora za završni ispit ("UML dijagrami — softverske klase, sekvence, komponente, paketa, deployment").

---

## Deliverable

| # | Dijagram | Izvor | Cilj |
|---|---|---|---|
| 1 | **Klasni dijagram** | `app/Models/`, `app/Enums/` | Domain model: Korisnik (apstraktan) → Profesor/Učenik/Administrator; Škola, Sport, Takmičenje, Ekipa, ČlanEkipe, LjekarskaPotvrda, Rezultat. Pokazati relacije iz spec §7.3 onako kako su STVARNO implementirane. |
| 2 | **Sequence dijagram UC5** | Pest test `tests/Feature/Teams/CreateTeamTest.php` + Service kod | Profesor → `TeamRegistrationController` → `TeamRegistrationService` → `MedicalCertificate::create` → `ValidateMedicalCertificateJob` (queue) → `FakeOcrAdapter` → `MedicalCertificate::update(status)` → notifikacija. |
| 3 | **Sequence dijagram UC8** | Pest test + Service kod | Admin → `StudentVerificationController` → `EDnevnikVerificationService` → `FakeEDnevnikAdapter::getStudent(jmb)` → poređenje → `Student::update(verification_status)` → audit log. |
| 4 | **Component dijagram** | Slojevita arhitektura iz spec §9.2 onako kako je STVARNO ispala | HTTP sloj (Controllers, Middleware, Form Requests) → Application (Services) → Domain (Models, Policies, Enums) + Infrastructure (Adapters, Repositories, Notifications). Eksterni: eDnevnik (mock), Google Vision (mock), SES (log). |
| 5 | **Package dijagram** | `app/`, `resources/js/`, `routes/`, `database/`, `tests/` struktura | Laravel app struktura kako je STVARNO organizovana (Routes split, Inertia React pages, factory/seeder paths). |
| 6 | **Deployment dijagram** | `composer.json` + `.env.example` + Laravel Cloud target | Razvojno okruženje (PHP + SQLite + log mailer + sync queue) i ciljano produkcijsko (Laravel Cloud + PostgreSQL + Redis + S3 + SES + Google Vision). |

---

## Pre-flight

- [ ] T3.2 merge-an u main
- [ ] Cijela aplikacija prolazi `php artisan test --compact`
- [ ] Worktree: `git worktree add ../sportski-savez-app-t4.1 -b feature/t4.1-uml-dijagrami`

---

## Alat za render

PlantUML preferiraj jer su izvori versionable u git-u. Mermaid kao backup za GitHub native rendering.

```bash
# Lokalni render (instalirati plantuml.jar ili koristiti VS Code extension)
plantuml docs/zavrsni-izvjestaj/uml/*.puml -o render/
```

---

## Acceptance criteria

- [ ] 6 `.puml` fajlova u `docs/zavrsni-izvjestaj/uml/` + 6 odgovarajućih PNG/SVG u `render/`
- [ ] Klasni dijagram ima svih 11 entiteta iz spec §7.1 sa stvarnim atributima iz migracija
- [ ] Sequence dijagrami imaju stvarne method potpise iz Service klasa
- [ ] Component dijagram pokazuje adapter pattern (interface + Fake* implementacija)
- [ ] Deployment dijagram razlikuje dev i prod environment
- [ ] Svi dijagrami čitljivi u svom render formatu
- [ ] PR opis link-uje na render slike

---

## Otvoreno za detaljisanje kroz `/plan`

- Alat za autogen iz Eloquent modela? (`barryvdh/laravel-ide-helper` ili rucno)
- Aspect ratio i veličina render slika
- Da li sequence dijagrami pokrivaju i alt tokove ili samo glavni tok